<header class="header">
  <a href="/" class="header-logo"><%= render 'layouts/logo' %></a>
  <ul class="header-nav">
    <% if logged_in? %>
    <li><%= link_to "Sign out (#{current_user.username})", logout_path, method: :delete %>
    <% end %>
  </ul>
</header>